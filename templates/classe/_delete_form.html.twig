<form method="post" action="{{ path('app_classe_delete', {'id': classe.id}) }}" onsubmit="return confirm('Voulez vous supprimer cette classe ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classe.id) }}">
    <br>
    <button >Supprimer</button>
</form>
