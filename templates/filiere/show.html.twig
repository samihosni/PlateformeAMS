{% extends 'template.html.twig' %}

{% block title %}Filiere{% endblock %}

{% block body %}
    <h1>Filiere</h1>

    <table class="table">
        <tbody >
            <tr>
                <th scope="col">Num</th>
                <td>{{ filiere.id }}</td>
            </tr>
            <tr>
                <th scope="col">Nom du filiere</th>
                <td>{{ filiere.nomFiliere }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_filiere_index') }}"><button>Retour</button></a>

    <a href="{{ path('app_filiere_edit', {'id': filiere.id}) }}"><button>Modifier</button></a>

    {{ include('filiere/_delete_form.html.twig') }}
{% endblock %}
