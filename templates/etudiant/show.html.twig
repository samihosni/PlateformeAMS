{% extends 'base.html.twig' %}

{% block title %}Etudiant{% endblock %}

{% block body %}
    <h1>Etudiant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant.id }}</td>
            </tr>
            <tr>
                <th>NomEtudiant</th>
                <td>{{ etudiant.nomEtudiant }}</td>
            </tr>
            <tr>
                <th>PrenomEtudiant</th>
                <td>{{ etudiant.prenomEtudiant }}</td>
            </tr>
            <tr>
                <th>AdresseMail</th>
                <td>{{ etudiant.adresseMail }}</td>
            </tr>
            <tr>
                <th>DateNaissance</th>
                <td>{{ etudiant.dateNaissance ? etudiant.dateNaissance|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>NumTelEtudiant</th>
                <td>{{ etudiant.numTelEtudiant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etudiant_index') }}">back to list</a>

    <a href="{{ path('app_etudiant_edit', {'id': etudiant.id}) }}">edit</a>

    {{ include('etudiant/_delete_form.html.twig') }}
{% endblock %}
