{% extends 'base.html.twig' %}

{% block title %}Enseignant{% endblock %}

{% block body %}
    <h1>Enseignant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enseignant.id }}</td>
            </tr>
            <tr>
                <th>NomEnseignant</th>
                <td>{{ enseignant.nomEnseignant }}</td>
            </tr>
            <tr>
                <th>PrenomEnseignant</th>
                <td>{{ enseignant.prenomEnseignant }}</td>
            </tr>
            <tr>
                <th>AdresseMailEnseignant</th>
                <td>{{ enseignant.adresseMailEnseignant }}</td>
            </tr>
            <tr>
                <th>NumTel</th>
                <td>{{ enseignant.numTel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enseignant_index') }}">back to list</a>

    <a href="{{ path('app_enseignant_edit', {'id': enseignant.id}) }}">edit</a>

    {{ include('enseignant/_delete_form.html.twig') }}
{% endblock %}
